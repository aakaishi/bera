import TextField from '../TextField';
import './Form.css';

const Form = (props) => {
  return (
    <section className='form'>
      <h3>Form</h3>
      <form onSubmit={}>
        <TextField />
      </form>
    </section>
  );
}

export default Form;  




