import './TextField.css'

const TextField = (props) => {
    return (
      <div className='text-field'>
          <label></label>
          <input value={} onChange={} required={} placeholder=''/>
      </div>
    );
}

export default TextField;